# :playback:base

This module provides shared types used by the Media3 playback service:
- `PlayerStatus` - the playback state enum.
- `MediaItemAdapter` - helpers to build Media3 `MediaItem`s for episodes.
- `RewindAfterPauseUtils` - computes how far to rewind after a pause/resume.
